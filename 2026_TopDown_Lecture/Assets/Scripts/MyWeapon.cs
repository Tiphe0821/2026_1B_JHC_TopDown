using UnityEngine;

public class MyWeapon : MonoBehaviour
{
    // 무기를 휘두르게 만들 스크립트. 애니메이션 재생으로 구현 시도 예정

    // 무기의 콤보는 총 3개. 여기서 필요한 것은 마우스를 눌렀을 때 다음 공격을 이어서 진행하는 것

    // 시작하기 앞서 인풋 시스템을 업데이트하자


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
